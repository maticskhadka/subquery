type StakingReward @entity{
  id: ID! #blockHeight-eventIdx
  account: String!
  balance: BigInt!
  date: Date!
  blockHeight: Int!
}

type SumReward @entity{
  id: ID! # accountId
  totalReward: BigInt!
  blockheight: Int!
}