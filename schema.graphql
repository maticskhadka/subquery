type StakingReward @entity{
  id: ID! #blockHeight-eventIdx
  account: SumReward!
  balance: BigInt!
  date: Date!
  blockHeight: Int!
}

type SumReward @entity{
  id: ID! # accountId
  totalReward: BigInt!
  blockheight: Int!
}